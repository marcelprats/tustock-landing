---
import Layout from '~/layouts/Layout.astro'; // Usamos Layout base (sin menús corporativos)

const { currentShop } = Astro.locals;
---

<Layout metadata={{ title: currentShop?.name }}>
  
  <div class="flex items-center justify-center min-h-screen bg-gray-50 dark:bg-slate-900 p-4">
    
    <div class="bg-white dark:bg-slate-800 w-full max-w-md p-8 rounded-2xl shadow-xl text-center border border-gray-100 dark:border-slate-700">
        
        {/* Icono Tienda */}
        <div class="mx-auto w-16 h-16 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center mb-6">
            <span class="text-3xl">🏪</span>
        </div>

        <h1 class="text-2xl font-bold text-gray-900 dark:text-white mb-2">
            {currentShop?.name}
        </h1>
        
        <div class="h-1 w-20 bg-blue-500 mx-auto rounded-full mb-6"></div>

        <p class="text-gray-500 dark:text-slate-400 mb-8 leading-relaxed">
            Esta tienda utiliza la tecnología de <b>TuStock</b> para su gestión, pero actualmente no tiene habilitada la venta online pública.
        </p>

        {/* Caja solo para el dueño */}
        <div class="bg-gray-50 dark:bg-slate-700/50 p-4 rounded-xl border border-gray-100 dark:border-slate-600">
            <p class="text-xs font-bold text-gray-500 uppercase tracking-wider mb-2">Administración</p>
            <a href="/admin" class="block w-full py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-bold transition text-sm">
                Acceder al Panel de Control
            </a>
        </div>

        <p class="text-[10px] text-gray-300 mt-6 uppercase tracking-widest">
            Powered by TuStock System
        </p>
    </div>

  </div>

</Layout>