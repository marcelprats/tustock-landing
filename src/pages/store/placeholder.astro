---
import Layout from '~/layouts/Layout.astro';

const { currentShop } = Astro.locals;
const shopName = currentShop?.name || "Tienda";
---

<Layout metadata={{ title: `${shopName} - Perfil` }}>
  
  <div class="relative min-h-screen flex items-center justify-center bg-page overflow-hidden">
    
    {/* Fondo Ambiental (Usa colores primarios del tema con opacidad) */}
    <div class="absolute inset-0 pointer-events-none" aria-hidden="true">
        <div class="absolute top-0 left-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl"></div>
        <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl"></div>
    </div>

    {/* Tarjeta Central "Glassmorphism" sutil */}
    <div class="relative z-10 w-full max-w-md bg-white dark:bg-slate-900 rounded-2xl shadow-xl border border-gray-200 dark:border-gray-700 p-1 mx-4">
        
        {/* Cabecera Degradada */}
        <div class="h-32 bg-gradient-to-r from-blue-600 to-indigo-600 rounded-t-xl relative">
            <div class="absolute -bottom-10 left-1/2 transform -translate-x-1/2">
                <div class="w-20 h-20 bg-page rounded-full p-1 shadow-lg ring-4 ring-page">
                    <div class="w-full h-full bg-blue-50 dark:bg-slate-800 rounded-full flex items-center justify-center text-3xl">
                        🏪
                    </div>
                </div>
            </div>
        </div>

        {/* Cuerpo de la tarjeta */}
        <div class="pt-12 pb-8 px-8 text-center">
            
            <h1 class="text-2xl font-bold font-heading text-heading mb-1">
                {shopName}
            </h1>
            
            <div class="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-300 text-xs font-bold mb-6">
                <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
                Verificado por TuStock
            </div>

            <p class="text-muted text-sm mb-8 leading-relaxed">
                Este comercio gestiona su inventario con nuestra tecnología, pero aún no ha abierto sus puertas digitales al público.
            </p>

            <div class="space-y-3">
                <a href="/admin" class="btn-primary w-full py-3 rounded-lg shadow-md flex justify-center items-center gap-2">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                    Soy el Propietario
                </a>
                
                <a href="https://tustock.app" target="_blank" class="btn-secondary w-full py-3 rounded-lg flex justify-center items-center gap-2">
                    <span>Crear mi tienda gratis</span>
                </a>
            </div>

        </div>

        {/* Footer Tarjeta */}
        <div class="py-4 text-center border-t border-gray-100 dark:border-gray-800 bg-gray-50 dark:bg-slate-800/50 rounded-b-xl">
            <p class="text-[10px] uppercase tracking-widest font-bold text-muted">
                Powered by <span class="text-primary">TuStock.App</span>
            </p>
        </div>

    </div>
  </div>
</Layout>