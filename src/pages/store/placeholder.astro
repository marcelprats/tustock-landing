---
import Layout from '~/layouts/Layout.astro';
import Hero from '~/components/widgets/Hero.astro';

const { currentShop } = Astro.locals;
---

<Layout metadata={{ title: `${currentShop?.name} en TuStock` }}>
  
  <div class="min-h-screen flex flex-col justify-center items-center bg-gray-50 dark:bg-slate-900 p-4">
    
    <div class="bg-white dark:bg-slate-800 p-8 rounded-2xl shadow-xl max-w-lg text-center border border-gray-100 dark:border-slate-700">
      <div class="mb-6 inline-flex items-center justify-center w-16 h-16 rounded-full bg-blue-100 text-blue-600">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" /></svg>
      </div>

      <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">
        {currentShop?.name}
      </h1>
      
      <p class="text-gray-500 mb-8">
        Este negocio gestiona su inventario digitalmente con la tecnología de TuStock, pero aún no ha habilitado su tienda online pública.
      </p>

      <div class="bg-blue-50 dark:bg-blue-900/20 p-4 rounded-xl mb-8">
        <p class="text-sm text-blue-800 dark:text-blue-200 font-medium">
          ¿Eres el dueño de {currentShop?.name}?
        </p>
        <a href="/admin" class="mt-2 inline-block px-6 py-2 bg-blue-600 text-white text-sm font-bold rounded-lg hover:bg-blue-700 transition">
          Acceder al Panel de Gestión
        </a>
      </div>

      <div class="border-t pt-6 border-gray-100 dark:border-slate-700">
        <p class="text-xs text-gray-400">Powered by <a href="https://tustock.app" class="font-bold hover:underline">TuStock.app</a></p>
      </div>
    </div>

  </div>
</Layout>